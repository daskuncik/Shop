@{
    Layout = "_Layout";
}

<h4>@ViewBag.Message</h4>

@if (ViewBag.UserId != null)
{
    <form asp-route="@ViewBag.Value.ToString()/addorder" method="post">
        <button>"Создать заказ"</button>
    </form>
}
