@{
    Layout = "_Layout";
}

<h4>@ViewBag.Message</h4>

@if (ViewBag.UserId != null)
{
    <form method="post" action="/@ViewBag.UserId.ToString()/addorder" >
        <input type="submit" value="Create order" />
    </form>
}
